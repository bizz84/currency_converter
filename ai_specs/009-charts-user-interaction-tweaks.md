The chart UI needs some tweaks:

When the selected point is null:
- The ExchangeRateHeader should show a green or red dot
- The timeRange should show the time range description
When the selected point is present:
- The ExchangeRateHeader should show a blue dot
- The timeRange should show the selected point's date
- The vertical line on the chart should remain visible, but the overlay should be hidden

Reference unfinished UI: `img/009-chart-select-unfinished.png`