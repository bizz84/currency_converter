Improve the currency selector screen so that it shows two sections with the following headers: RECENT, ALL.

- First section should show all the currencies already in the convert screen, followed by previously used currencies in most recently used order
- Second section should show all the available currencies

Notes:
- Currencies in RECENT can appear in ALL
- Current currency always has a checkmark (same as now)

Reference the sample UI design for the list layout: `img/currency-selector-preview.jpeg`