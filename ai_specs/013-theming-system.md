Goal: Introduce a theming system to the app that can be used to avoid hardcoding colors and styles across the codebase.

Begin by inspecting all the widget classes. Identify duplication across a number of areas:

- text styling
- paddings and spacing
- colors (primary, secondary, foreground, background, etc.)

Make a list of all the places where these duplications occur so I can review.
