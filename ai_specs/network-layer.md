Create a `FrankfurterClient` class that can be used to make requests to the Frankfurter API.

Requirements:

- Save the class in the `lib/network/frankfurter_client.dart` file
- This class should take a `Dio` client as a dependency
- Add methods for all relevant endpoints as defined in the `rest-api/frankfurter.http` file

