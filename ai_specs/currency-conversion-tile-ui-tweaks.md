
Apply the following UI tweaks to the `CurrencyConversionTile`:

- Tile should be edge-to-edge (without using a Card). This fits better with the Dimissible widget.
- Background should be off-white. Foreground widgets should be white so they stand out more.
- Remove the `X` icon from each tile.
- Converted value should include the currency symbol (e.g. "€ 61.63" rather than "61.63")
- Move the subtitle to the trailing side of the tile (e.g. "1 USD = 0.85 EUR")
- Flag and Currency name should be vertically aligned

Use `ai_specs/img/convert-preview.jpeg` as a reference (but don't include the three dots icon in the tile).